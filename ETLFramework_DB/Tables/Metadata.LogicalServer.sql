CREATE TABLE Metadata.LogicalServer (
    ServerID          int          NOT NULL,
    ServerDescription varchar(50) NULL,
    CONSTRAINT PK_Metadata#LogicalServer PRIMARY KEY CLUSTERED (ServerID ASC)
);



